# =====================================================================
# LOCAL MySQL (Development) - COMMENTED OUT
# =====================================================================
#spring.datasource.url=jdbc:mysql://localhost:3306/jolly-jobs-dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=fulcrum
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#server.port=8081
#jwt.secret=MySuperSecretKeyForJWTGeneration1234567890


# =====================================================================
# PLANETSCALE (Production on Render)
# =====================================================================

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Render dynamically injects PORT
server.port=${PORT:8080}

jwt.secret=${JWT_SECRET}
